apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: app-search-demo-ingress
  namespace: <%= app_name %>
spec:
  rules:
    - host: 'packagehunt.swiftype.info'
      http:
        paths:
          - backend:
              serviceName: node-backend
              servicePort: 3000
